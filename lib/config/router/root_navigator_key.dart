import 'package:flutter/material.dart';

/// Root navigator key used for global navigation (e.g. force logout on 401).
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();

